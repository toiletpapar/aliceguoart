import React from 'react'

import {
  BlockContainer,
} from '../BasicComponents/index'

const Sidebar = (props) => {
  return (
    <BlockContainer>
      Hello Sidebar!
    </BlockContainer>
  )
}

export default Sidebar
