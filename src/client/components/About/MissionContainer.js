import styled from 'styled-components'

import {
  ViewPortContainer,
} from '../BasicComponents/index'

const MissionContainer = styled(ViewPortContainer)`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 0px 15%
  max-height: 275px
`

export default MissionContainer
