import React from 'react'

import {
  BlockContainer,
} from '../BasicComponents/index'

const About = (props) => {
  return (
    <BlockContainer>
      Welcome to About!
    </BlockContainer>
  )
}

export default About
