import React from 'react'

import {
  BlockContainer,
} from '../BasicComponents/index'

const Header = (props) => {
  return (
    <BlockContainer>
      Hello World!
    </BlockContainer>
  )
}

export default Header
