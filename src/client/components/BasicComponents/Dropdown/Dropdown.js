import React from 'react'

import {
  BlockContainer,
} from '../index'

// Usually used for menus. Anything more complicated will require react-select component
const Dropdown = (props) => {
  return (
    <BlockContainer>Hello World</BlockContainer>
  )
}

export default Dropdown
