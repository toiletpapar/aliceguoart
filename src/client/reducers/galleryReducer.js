import { galleryState as initialState } from './InitialStates'

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state
    }
  }
}

export default reducer
